{
  "name": "@funcode/quest-template",
  "version": "0.1.0",
  "private": true,
  "description": "Standard quest template scaffold for FunCode quests",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2",
    "typescript": "^5",
    "@types/node": "^20"
  }
}
