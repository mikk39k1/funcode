/**
 * Quest: {{QUEST_TITLE}}
 *
 * Your task: {{QUEST_DESCRIPTION}}
 *
 * Export a single function called `solution` that accepts the input
 * described below and returns the expected output.
 *
 * The test runner in `tests/runner.test.ts` will call `solution(input)`
 * for each test case and compare the result against `expectedOutput`.
 */

/**
 * @param input - {{INPUT_DESCRIPTION}}
 * @returns {{OUTPUT_DESCRIPTION}}
 */
export function solution(input: unknown): unknown {
  // ✨ Your implementation goes here
  throw new Error("Not yet implemented");
}
